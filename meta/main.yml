galaxy_info:
  author: Amrit Singh (amritsingh)
  description: An ansible role to install, configure the AWS CloudWatch Logs and setup systemd service
  company:
  license: "license (BSD, MIT)"
  min_ansible_version: 0.1
  platforms:
    - name: Ubuntu
      versions:
      - all
  galaxy_tags:
    - awslogs
    - system.d
    - systemd
    - system
    - cloud
    - cloud:ec2
dependencies: []
